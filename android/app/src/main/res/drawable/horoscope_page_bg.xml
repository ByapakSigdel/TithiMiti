<?xml version="1.0" encoding="utf-8"?>
<!-- Warm "notebook page" the written reading sits on. Paper ground with a faint
     warm vignette for depth; theme-aware via the widget color tokens, with a
     1dp sand hairline so it reads as the same card family as the other
     widgets. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/widget_card" />
            <corners android:radius="24dp" />
            <stroke android:width="1dp" android:color="@color/widget_border" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="24dp" />
            <gradient
                android:type="radial"
                android:centerX="0.5"
                android:centerY="0.42"
                android:gradientRadius="80%"
                android:startColor="#00000000"
                android:endColor="#144A3B1E" />
        </shape>
    </item>
</layer-list>
