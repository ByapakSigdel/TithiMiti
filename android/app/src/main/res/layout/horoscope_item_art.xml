<?xml version="1.0" encoding="utf-8"?>
<!-- The painting "page": a full-widget-height image you see first. Scrolling it
     up reveals the horoscope panel below. Falls back to the mood gradient until
     a real Met painting has downloaded. -->
<ImageView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/art_row"
    android:layout_width="match_parent"
    android:layout_height="150dp"
    android:scaleType="centerCrop"
    android:contentDescription="@null"
    android:src="@drawable/horoscope_art_airy" />
