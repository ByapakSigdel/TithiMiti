<?xml version="1.0" encoding="utf-8"?>
<!-- Minimal metal-prices widget: just the two prices on a dark card. Gold price
     in gold, silver price in silver — the colour is the only label needed. A
     short hairline separates them. Matches the custom-event widget's size/style. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_bg_metals"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="12dp">

    <TextView
        android:id="@+id/gold_price"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="—"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="#FFE9C46A"
        android:letterSpacing="0.01"
        android:maxLines="1"
        android:ellipsize="end" />

    <ImageView
        android:layout_width="34dp"
        android:layout_height="1dp"
        android:layout_marginTop="6dp"
        android:layout_marginBottom="6dp"
        android:contentDescription="@null"
        android:background="#22FFFFFF" />

    <TextView
        android:id="@+id/silver_price"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="—"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="#FFC8CCD0"
        android:letterSpacing="0.01"
        android:maxLines="1"
        android:ellipsize="end" />

</LinearLayout>
